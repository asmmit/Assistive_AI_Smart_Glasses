opencv-python>=4.8.0
ultralytics>=8.1.0
numpy>=1.24.0
mediapipe>=0.10.9
Pillow>=10.0.0
scipy>=1.11.0
# pygame is optional – on Python 3.14 Windows it often fails to install.
# The code falls back to console beeps / winsound automatically.
